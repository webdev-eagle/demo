export interface DuckAchievements {
    a: string[];
    n: number | null;
}

export interface TurtleAchievements {
    a: string[];
    n: number | null;
}

export interface MutantAchievements {
    a: string[];
    n: number | null;
}
